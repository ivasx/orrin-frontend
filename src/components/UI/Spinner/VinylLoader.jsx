export default function VinylLoader() {
    // Without CSS, just using global classes from index.html
    return (
        <div className="orrin-loader-container">
            <div className="vinyl-record"></div>
        </div>
    );
}